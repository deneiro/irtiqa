/**
 * English text produced by game logic rather than by a component: tutorial script,
 * chronicle narration, insights, engine/store toasts. Filled in alongside those modules.
 */
import type { Dict } from '..';

export const EN_GAME: Dict = {};
